export type dep_Theme = {
    primaryText: string,
    secondaryText: string,
    primaryBackground: string,
    primaryXAxis: string,
    secondaryXAxis: string,
    primaryYAxis: string,
    secondaryYAxis: string,
    primaryPath: string,
    primaryWarning: string,
}

export type Theme = "theme-basic"